info:
  name: Delete Courier
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/couriers/{{courierId}}"
  headers:
    - name: accept
      value: application/json
  body:
    type: none
  auth:
    type: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deletes the courier", function() {
          expect(res.status).to.equal(200);
          expect(res.body).to.deep.equal({
            success: true,
            message: "Courier deleted successfully.",
            data: null
          });
        });

settings:
  encodeUrl: true
